# Supabase Configuration
# Get these values from: Supabase Dashboard > Settings > API

# Project URL (Required)
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Anonymous Key (Required - Safe to expose in frontend)
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# Service Role Key (Required for Admin Operations)
# WARNING: This key has admin privileges!
# - Get it from: Supabase Dashboard > Settings > API > service_role secret
# - NEVER commit this to version control
# - Keep it secure and rotate regularly
VITE_SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Production Notes:
# - Session storage: Memory-only (no localStorage)
# - Authentication: Production Supabase only (no dev fallbacks)
# - Admin credentials: Set via Supabase Auth UI or SQL